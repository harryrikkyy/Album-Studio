# ── Secrets — never commit ────────────────────────────────
.env
.env.*
!.env.example
client_secret_*.json
*.credentials.json
.ch_toolkit_license

# ── Node / build ──────────────────────────────────────────
node_modules/
dist/
*.log

# ── OS / editor cruft ─────────────────────────────────────
.DS_Store
*.swp
.vscode/
.claude/

# ── Python cache (from steering skill scripts) ────────────
__pycache__/
*.pyc

# Impeccable design tool (separate checkout)
impeccable/

# Serena MCP tool metadata (per-developer, not project state)
.serena/

# Playwright
test-results/
playwright-report/

# License signing — the private key must never be committed
server/license_signing_key.pem

# Renderer bundle (built by npm run build:renderer)
src/dist/

# Phase 4 benchmark fixture (generated)
bench/fixture/
